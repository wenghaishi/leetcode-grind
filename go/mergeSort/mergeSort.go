package main

func mergeSort(arr []int) []int {
	length := len(arr)
	if length <= 1 {
		return
	}

	middle := length / 2

	leftArr := arr[:middle]
	rightArr := arr[middle:]

	i,j := 0,0

	for; i < length; i++ {

	}
}

func merge(leftArr []int, rightArr []int, arr []int) {

}
